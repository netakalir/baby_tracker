import { SettingsPlaceholder } from './SettingsPlaceholder'

/** Placeholder for "Profile & account" (built in slice 12d). */
export function ProfileScreen() {
  return <SettingsPlaceholder title="פרופיל וחשבון" />
}
