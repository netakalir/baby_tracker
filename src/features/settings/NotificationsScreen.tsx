import { SettingsPlaceholder } from './SettingsPlaceholder'

/** Placeholder for "Notifications" (built in slice 12g). */
export function NotificationsScreen() {
  return <SettingsPlaceholder title="התראות" />
}
