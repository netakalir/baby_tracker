import { SettingsPlaceholder } from './SettingsPlaceholder'

/** Placeholder for "Display & language" (built in slice 12f). */
export function DisplayScreen() {
  return <SettingsPlaceholder title="תצוגה ושפה" />
}
