# Dependencies
node_modules/

# Build output
dist/
build/
dev-dist/

# Playwright E2E artifacts
test-results/
playwright-report/
.playwright/

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
# E2E test env - holds the Supabase service_role key, must never be committed
.env.test

# Local MCP config (contains API tokens)
.mcp.json

# Local Claude Code settings
.claude/settings.local.json

# OS / editor
.DS_Store
*.swp
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Vite
dist-ssr

# Agent worktrees (isolated subagent branches live here)
.claude/worktrees/

# Dev-only seed scripts (read the service_role key from .env.test, not for prod)
.seed-events.mjs
.seed-preview.mjs
